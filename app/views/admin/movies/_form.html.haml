= simple_form_for([:admin, @movie]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :summary
    = f.input :poster, as: :file

  .form-actions
    = f.button :submit
