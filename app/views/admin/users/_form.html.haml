= simple_form_for([:admin, @user]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email
    = f.input :name
    = f.input :password
    = f.input :avatar, as: :file
  .form-actions
    = f.button :submit
